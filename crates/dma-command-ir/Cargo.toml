[package]
name = "dma-command-ir"
version = "0.1.0-os.1"
authors = ["Ryan Stephens"]
edition = "2024"
rust-version = "1.89"
description = "Allocation-free typed DMA command intermediate representation and validator"
license-file = "../../LICENSE"
publish = false
[lib]
path = "src/lib.rs"
[features]
default = []
