[package]
name = "storage-guardian-contract"
version = "0.1.0-os.1"
authors = ["Ryan Stephens"]
edition = "2024"
rust-version = "1.89"
description = "Fail-closed native storage mutation, durable journal, containment, and recovery contract"
publish = false
[lib]
path = "src/lib.rs"
[features]
default = []
